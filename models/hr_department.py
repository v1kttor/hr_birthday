# Comments

from odoo import fields, models, api

class Department(models,Model):
    _name = hr_birthday.department

    check_birthdays = fields.Boolean(default=True)
    birthday_remind_days = fields.Integer()

    
